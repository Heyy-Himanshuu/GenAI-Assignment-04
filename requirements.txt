# Core dependencies for the Claude-powered vision web-automation agent.
#
# Install with:   pip install -r requirements.txt
# Then run:       playwright install chromium

anthropic>=0.40.0       # Anthropic Claude SDK (vision + tool use)
playwright>=1.40.0      # Browser automation (Chromium)
python-dotenv>=1.0.0    # Loads ANTHROPIC_API_KEY and settings from .env
